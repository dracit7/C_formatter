
/* This is the input file for testing. */
#include "testing/sampleHeader.h"

// global declarations
int integer1;
double sample_float = 0.98;
long a[10] = 8,b = 9L;
short p2p = 0x9;

void print(char c) {
  putchar(c);
  return;
}

int main() {
char c = '8', d= '6';  if (c - d == 2) {
if (c != 1) ;


    return /* */ 1;
  } else {} while (c >= 0 && 1 || d != 3) print(c);
  for(int i = 0; i<100; i=i+1) {
    for (; i<10 ; i=i+1) {
      break;
    }
    print(i);
    continue;
    break;
  }
  print('n');
  return 0;
}

int testOct = 010;
int textHex = 0xBee10;
